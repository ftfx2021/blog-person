# P0 习惯模块体验与完整性修复任务书（Prompt）· 表单校验 + 完整编辑 + 补卡/撤销历史 + 暂停归档 + 周进度 · 给搭建智能体

> 用法：把**整篇**内容作为对话/prompt 发给搭建智能体（全栈开发方向）。工作目录=工程根 `D:\javaweb\个人博客\人生系统\life-system`（注意：**不是**项目根）。  
> 前置：P0 已交付（习惯服务层/schema/页面存在）。本任务以**前端体验修复**为主，附带**服务端两处最小扩展**（list 返回今日打卡标记；habit 表加 status 列支持暂停/归档），禁止改已验收的打卡/撤销/历史/streak 计算逻辑。  
> 本任务书**自包含**；若能读文件，用法见文末。

---

##

一、角色

## 用法说明（发给智能体前看）

- 本任务书**自包含**，直接发送全文即可；改动 = `HabitsPage.vue` + `habits.service.ts`（list/updateStatus）+ contracts（status schema）+ 迁移 007。
- 服务端 `checkin`/`undo`/`history`/`update`/streak 算法**已验收，一个不碰**——补卡/撤销只是新增前端入口。
- 验收以"每个操作都有明确反馈 + 补卡/撤销/暂停/归档可用 + 首屏单请求 + 周进度可见"为准。
