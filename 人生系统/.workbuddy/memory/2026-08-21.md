# 2026-08-21 工作日志

## 对话恢复
- 用户要求恢复项目历史对话
- 从本地 JSONL 文件中成功提取了 3 个历史会话的完整用户消息
- 主会话 `71d66ee1`（7.8MB, 84 条用户消息）覆盖项目全生命周期
- 当前停点：P1-1 已出任务书 + 基础问答任务书已交付，用户已确认 "OK"
- 对话无法直接导入当前 UI，但已通过 MEMORY.md + 项目文档完整恢复上下文

## P1-2 任务书交付
- 产出 `docs/prompts/P1-2任务书.md`（Milvus 集成 + 连接测试/健康 + 切入备份/恢复）
- 关键锁定：官方 SDK `@zilliz/milvus2-sdk-node@^2.5.0`（唯一新依赖，gRPC，区别于 P1-1 手搓 HTTP）；`knowledge_chunk_v1` collection 维度配置化（来自 embedding 模型 dim，不写死 512）；错误码新增 `VECTOR_DB_UNAVAILABLE`；manifest v2 向后兼容 v1；备份=MysQL dump+Milvus 全量导出 JSON+原件副本；恢复=安全点+drop&recreate+upsert+chunk 数一致性校验
- 已同步更新 `docs/开发计划.md` §3.2 P1-2 状态为"任务书已交付，待搭建回收"
- 工程现状确认：P1-1（llm/embedding）与基础问答（chat/assistant）均已搭建进 life-system

## P1-8 AI 助手体系（决策 + 计划 + 任务书）
- 用户要求 AI 助手扩展为类 Cherry Studio：助手→会话→历史三层；未来要上下文管理/记忆/工具系统
- 产出 `docs/decisions/P1-AI助手扩展决策.md`：三张新表（ai_assistant/ai_session/ai_message）；**T6 覆写**（问答历史由"不持久化"改为持久化，FSD 待确认清单已同步）；A1~A5 拍板（助手仅 name/description/system_prompt，model_config_json/knowledge_base_id 预留列；上下文=system prompt+最近20条；用户消息即落库、回复完成后落库、LLM 失败回滚用户消息；自动命名=首条消息前20字）
- 产出 `docs/prompts/P1-8任务书.md`：迁移 002_ai_assistant.sql + Drizzle schema、assistant service CRUD、ChatService 改造（DB 上下文+落库+回滚+中止半条入库）、IPC/preload、AssistantPage 双栏改造
- 更新 `docs/开发计划.md`：P1-8 加入 §3.2（✅ 任务书已交付，可并行，不依赖 P1-2~7）；新增"AI 能力深化路线"（助手进阶配置/上下文管理/记忆系统/工具系统）
- 零新增依赖；chatStartSchema 改 sessionId 必填
- **状态更新（当日下午）**：开发计划显示 P1-8 已由用户搭建完成（✅ 已实现并完成 typecheck/lint/test/build）

## P1-3 收藏箱（决策 + 任务书）
- 用户问收藏箱形态 → 解释：支持 link/snippet/read_later 三类，网页只是其一；文件导入不走收藏箱（属 K02）；snippet 转文档路径在 FSD 是缺口
- 产出 `docs/decisions/P1-收藏箱决策.md`：B1~B5（网页抓取+K02-F10 统一入库属 P1-4；P1-3 定义 `knowledgeService.clipToDocument` 端口占位抛 `INGEST_UNAVAILABLE`，P1-4 替换实现——并行开发契约；snippet 允许剪藏入库=note 内容转 doc_type=note；错误码新增 `INGEST_UNAVAILABLE`）
- 产出 `docs/prompts/P1-3任务书.md`：inbox service（CRUD+状态机+剪藏入口）、knowledge/index.ts 端口契约、contracts/IPC/preload、InboxPage.vue（对齐 prototype/inbox.html 结构）+ 路由 /inbox；零新增依赖
- 更新开发计划 P1-3 状态 ✅ + FSD 待确认清单补 snippet 转文档已拍板
- 原型 reference：prototype/inbox.html（Tab 分组 + 剪藏/仅留/丢弃 + 处理规则说明）

## P1-8 修复任务书（AI 助手体验细粒度修复）
- 用户要求收藏箱 prompt + AI 助手细粒度修复 prompt；收藏箱复用已交付的 P1-3任务书.md（未重复产出）
- 读完 P1-8 现有代码，定位核心缺陷：**stop() 只 abort 业务层 controller，openai-compatible.ts 的 fetch 用独立超时 controller 不感知外部 signal → 停止后底层请求仍在跑**
- 产出 `docs/prompts/P1-8修复任务书.md`，六大修复点 F-1~F-6：
  - F-1 停止立即中断：ChatRequest.signal 贯通 fetch（合并信号 + 外部 abort 不重试）
  - F-2 思考模式开关：chatStartSchema 加 think；只控展示/持久化，不传服务端参数（供应商无统一协议，避免厂商特判）
  - F-3 推理落库：迁移 003 ai_message 加 reasoning 列
  - F-4 Markdown 渲染：markdown-it + dompurify（渲染层新增依赖），v-html 仅经 renderMarkdown 清洗
  - F-5 思考区渲染：details 折叠面板；页面补订阅 onReasoning（当前未订阅）
  - F-6 首字探测（TTFB 计时 + 30s 超时仅提示）+ rAF 合并 delta
- 关键设计约束：外部 abort 不重试；思考开关不传 reasoning_effort（DeepSeek 无开关、OpenAI o 系必须思考，无统一协议）

## P1-8 修复任务书扩展（F-7~F-17，用户要求 A/B 档全并入）
- 用户确认把改进建议 A/B 档 + 思考 token 可视化/推理链展开全部并入 P1-8 修复任务书
- 新增 F-7~F-17：
  - F-7 超时分层 connect10s/首字60s/流空闲30s + 错误码 AI_TIMEOUT（修复长思考模型被 30s 总超时掐断）
  - F-8 token 预算上下文 6000（字符估算，不引 tiktoken，兜底 40 条）
  - F-9 消息操作（复制/重新生成/删末条/清空会话）+ 新 IPC messages:remove/sessions:regenerate/sessions:clear
  - F-10 断流自动重试 1 次（非用户中止，整轮重发静默）
  - F-11 全局并发限 2 条（CONFLICT 拒绝，不排队）
  - F-12 usage 透传（含 reasoning_tokens）+ 气泡角标 + 模型名
  - F-13 虚拟滚动（el-virtual-list 优先）+ markdown 渲染 LRU 缓存
  - F-14 会话搜索/置顶（迁移 004 pinned 列）/导出 Markdown/清空
  - F-15 失败重试按钮
  - F-16 Electron Notification（无聚焦窗口时发）
  - F-17 思考 token 展示 + 推理链展开/摘要（前 200 字）
- 迁移变为 003（reasoning）+ 004（pinned）；依赖仍仅 markdown-it+dompurify

## 验收：P1-3 收藏箱 + P1-8 体验修复（通过）
- 用户完成搭建，请求校验。逐文件核对 + 构建验证全部通过
- P1-3 ✅：inbox service（状态机/行锁/snippet 规则）、knowledge/index.ts 端口占位、contracts/IPC/preload、InboxPage（Tab 分组/剪藏降级 INGEST_UNAVAILABLE）、路由 /inbox
- P1-8 修复 ✅：F-1~F-17 全部落地（signal 贯通 fetch、超时三档+AI_TIMEOUT、token 预算 6000、消息操作、断流重试、并发限 2、usage+model 透传、虚拟滚动窗口截断、置顶/导出/清空、重试按钮、通知、思考 token/推理链展开、迁移 003/004）
- 构建：typecheck ✅ / lint 0 error（1722 格式类 warnings 可 --fix）/ test 13/13 ✅ / 边界+IPC schema ✅ / 依赖干净（仅 markdown-it+dompurify，无框架）/ 无模板句灌水
- 小瑕疵（不阻塞）：exportSessionMarkdown 只导出最近 200 条（任务书要求全部）；删除消息/重新生成后未更新 session.updated_at
- 附带发现：P1-2（Milvus）也已交付并入（infrastructure/milvus/ + settings:milvus:* + 备份向量导出 + milvus sdk 2.6.17），一并标注已实现
- 已更新 docs/开发计划.md：P1-2/P1-3/P1-8 状态 → 已实现

## 超时参数修正（用户反馈）
- 用户指出 connect10s/首字60s/流空闲30s 太激进，慢模型/深度思考会被误杀
- 修正 P1-8修复任务书 F-7：默认 connect15s/首字90s/流空闲60s；**think=true 首字放宽 300s**；ChatRequest.timeouts 可覆盖；UI 30s 提示仅提示不取消（与主进程超时解耦）
- 已同步 §九 技术决策表；搭建模型需按新值微调 openai-compatible.ts 常量 + chat service 按 think 传 timeouts

## 拆分独立修复任务书（用户要求）
- 用户要求：超时修正不放 P1-8 修复任务书（已回滚该文件 F-7/§九 的修改，仅加引用注）；Markdown 渲染用现成库不要手搓
- 产出 `docs/prompts/AI助手超时与渲染修复任务书.md`（T-1/T-2）：
  - T-1 超时放宽：connect15s/首字90s/流空闲60s，think=true 首字 300s，ChatRequest.timeouts 覆盖
  - T-2 渲染换 Vditor（现成库，内置高亮+sanitize+样式），删除 markdown.ts 手搓封装 + 卸载 markdown-it/dompurify；新建 MarkdownView.vue；思考区保持纯文本
- 与架构选型对齐说明：Milkdown 留给未来编辑（K02-F06），Vditor 管只读渲染

## 任务书再更新（会话标题异步生成）
- 用户要求：会话标题按第一轮对话内容异步生成（LLM），替代截取式
- 任务书改名 `AI助手超时与渲染修复任务书.md` → `AI助手补充修复任务书.md`（新增 T-3 标题生成），P1-8修复任务书.md 引用同步更新
- T-3 设计：移除 appendMessage 截取式命名；新增 generateSessionTitle（仅 title='新会话' 时，读首轮 user+assistant 各截 2000 字 → LLM 非流式 maxTokens 50 → ≤30 字清洗后更新）；chat service run 完成后 fire-and-forget 触发；失败静默不重试；主进程广播 sessions:title-updated 事件 + preload onTitleUpdated + UI 原地更新

## 超时检查结论（T-1 不执行）
- 用户要求先检查超时是否已修复，满足则不动
- 检查结果：openai-compatible.ts 已被改为 **connect 10s / 首字无限等待（receivedEvent 前直接 reader.read() 无超时）/ 流空闲 300s**——比建议方案（15s/90s+think300s/60s）更宽松，慢模型/深度思考误杀问题已解决 ✅
- 已从 AI助手补充修复任务书移除 T-1（超时），保留 T-1=Vditor 渲染、T-2=标题生成；任务书头部加"检查结论"溯源；硬约束明确"不改超时相关代码"

## P1-4 讨论补充（网页链接导入 + 平台接入声明）
- 用户要求：文档导入加网页链接解析；声明后续支持飞书/语雀等主流文档接入
- 设计确认：知识库页「导入」分两类（导入文件 / 导入网页链接），均汇入 K02-F10；网页抓取与收藏箱剪藏共用同一 HTML 解析实现；URL 校验/抓取超时/失败保留来源
- 平台接入 = 未来方向声明（本期不实现，FSD 基线"不调用平台 API"）：开发计划未来路线加"主流文档平台接入（飞书/语雀，平台适配器方式）"；设计文档 1.4 采集方式表同步更新
- 待用户拍板（上轮 4 项）：手写编辑器（textarea+预览 vs Milkdown）、文件大小/解析超时默认值、标签与文档管理 UI 范围、分块 token 估算共享

## P1-4 文档导入（拍板 + 任务书交付）
- 用户拍板 4 项：Milkdown 编辑器 / 20MB+60s 可配置 / 文档管理 UI 完整做 / token 估算入 shared/domain 共用
- 产出 `docs/decisions/P1-文档导入决策.md`：P4-1~P4-7（范围、P1-5 边界、编辑器、大小超时、UI、token 共享、平台声明）+ 解析器锁定 + K02-F10 防简化硬规定
- 产出 `docs/prompts/P1-4任务书.md`（P1 最大任务书）：五解析器（pdfjs-dist/mammoth/unified/cheerio+turndown/TXT流式）、K02-F10 编排（元数据壳/幂等/单事务/失败保留/触发占位）、替换 clipToDocument 占位（收藏箱打通）、文档管理服务+UI、Milkdown 编辑器、设置项、shared/domain/tokens.ts
- 防简化红线内嵌：禁 pdf-parse/禁 zip 正则/禁固定长度分块/禁跳过壳/禁绕过 K02-F10；每格式"禁止"列
- 更新开发计划 P1-4 状态 ✅ 任务书已交付

## 开发计划新增 §1.1 目标与计划设计基线
- 用户疑虑"目标与计划设计的开发计划去哪了"→ 核查：开发计划从未删改章节（7 章齐全），目标设计实际在设计文档 §1.2/§2.1
- 用户要求纳入开发计划：新增 §1.1「目标与计划设计基线」（已拍板，来源设计文档）：必备实体=目标+待办；目标不建层级用 period 字段；目标待办可空外键松耦合；三种度量型（数值/里程碑/状态）；投入≠结果；无四象限；愿景暂缓；领域用标签
- 版本号 v1.0 → v1.1
- 附注：docs/ 未被 git 追踪，无版本历史，已提醒用户考虑纳入版本管理

## §1.1 改为 P1/P2 表格式
- 用户要求目标与计划设计基线写成类似 P1/P2 的步骤表格式
- 重写 §1.1 为 P0-1~P0-10 表格（步骤|设计决策|功能编号|为什么这样定|状态）：目标单层+period、三种度量型、投入≠结果、目标待办松耦合、无四象限、习惯独立、生活项目可选、领域用标签、愿景暂缓(P3)、无日程实体
- 标题保留用户手改的"P3 阶段实现依据"字样；表格说明注明 P0 已实现 + P3 未来方向

## P0 目标模块体验审查（用户反馈粗糙/保存无反应）
- 审查结论：服务层（校验/事务/状态机/schema）完整，问题全在前端
- 具体问题：
  1. GoalsPage 新建表单无 el-form rules（标题空/起终点相等都能点保存，靠服务端返回才弹 toast）
  2. save() 无 catch 兜底（错误依赖 call 内部 ElMessage）；无成功反馈
  3. GoalDetailPage edit() 只有标题 prompt + **tags:[] 硬编码 → 编辑保存后标签被清空（bug）**
  4. finish()/remove() 的 ElMessageBox.confirm 取消时 reject 未捕获 → 取消弹窗像"没反应"
  5. addRecord/addMilestone 无前端校验；record.value 默认 0 可直存
  6. 列表页无搜索/状态筛选 UI（服务端已支持 status/keyword）
  7. 全部写操作无 try/catch，统一依赖 call 内部 toast

## AI 助手"退出后只剩推理"排查（根因定位）
- 症状：软件退出后历史记录正文没了，只剩推理（思考区）
- 根因：chat/service.ts 落库条件 `if (active.text || active.reasoning)` ——思考模式流被中断（退出/停止）时正文未开始（text=''）但 reasoning 非空 → 落库 content='' + reasoning 半条 → 重启渲染正文空白只剩思考区
- 修复方案（决策）：R-1 落库条件收紧为 `if (active.text)`（正文为空不保存，推理无正文无保留价值；已生成部分正文的中断仍保存）；R-2 UI 兜底（content 空+reasoning 非空 → 显示"回答未完成，已中断"占位，不渲染空白）
- 补充修复（Vditor/标题生成）已实现并入工程

## AI 助手 Markdown 样式修复任务书
- 用户肉眼反馈：段落间距过大、代码块太丑（Vditor light 默认深底+行号）
- 拍板：1A 代码块浅底深字（#f6f8fa/#24292f GitHub 风）、2B 关闭行号
- 产出 docs/prompts/AI助手Markdown样式修复任务书.md：只改 MarkdownView.vue（hljs lineNumber:false + scoped :deep() 覆盖 p 4px/pre 浅底/行内代码/标题/列表/引用/表格）

## 知识库增强（拍板 + 任务书交付）
- 用户拍板三项：1) 扫描件 OCR 两个都做（在线百度/腾讯/阿里可配置 + 本地 tesseract 兜底，默认优先在线）；2) 网页 403 上 Electron Chromium 渲染抓取；3) 原件打开/另存确认加入
- 产出 docs/decisions/P1-知识库增强决策.md（K-1/K-2/K-3 设计：三家手搓签名、PDF 转图 @napi-rs/canvas、Chromium 隐藏窗口隔离 session、原件 shell.openPath/dialog+copyFile）
- 产出 docs/prompts/知识库增强任务书.md（E-1~E-6）：ocr 目录+三在线签名+tesseract+调度、PdfParser 抛 OCR_REQUIRED、ingest 接入 OCR、Chromium 抓取替换 fetchWebpage、原件 IPC+UI、knowledge.ocr 设置项（safeStorage）
- 新增依赖：@napi-rs/canvas（原生需 rebuild）、tesseract.js（WASM 语言包本地化）；禁云 SDK 手搓签名

## 知识库增强任务书更新（OCR 官方文档核实）
- 用户提醒：三家在线 OCR 需先查官方文档再实现，别急着定接入方法
- 联网核实三家要点并修正任务书：
  - 百度：accurate_basic + OAuth2 token（30 天缓存）；**支持 pdf_file 直传**（可跳过逐页转图）
  - 腾讯：ocr.tencentcloudapi.com + TextRecognize + V2018-11-19 + TC3 V3 签名（超 1M 必须 V3）
  - 阿里：ocr-api.cn-hangzhou.aliyuncs.com + RecognizeGeneral + **V2021-07-07（原写 2019-12-30 错误）** + HMAC-SHA1 RPC 签名；官方明确不支持 PDF
- 任务书 §5.1.1 已更新：官方文档链接 + "实现前必读，以官方最新文档为准，冲突记入 decisions/OCR-接入核实记录.md"；验收加"官方文档核实"项

## 待办模块审查 + 修复任务书
- 用户要求审查待办模块完整性/体验，只指问题不给改法
- 审查结论（服务端完整，问题在前端）：编辑不完整（只改标题）、筛选未用满（服务端支持 goalId/projectId/dateFrom/dateTo 页面只有状态）、无关键词搜索（服务端无 keyword）、无逾期感知；体验同目标模块（无 rules/无成功反馈/确认框取消未捕获/流转无防重复）
- 产出 docs/prompts/P0待办体验修复任务书.md（R-1~R-8：表单校验+共用编辑弹窗+反馈+取消捕获+防重复+筛选增强+逾期标记+可读性）

## 生活项目模块审查 + 修复任务书
- 用户要求审查 projects 模块完整性/体验，只指问题不给改法
- 审查结论（服务端完整，问题在前端，且比目标/待办多两个独有严重 bug）：
  1. **编辑保存清空标签（数据丢失）**：edit() 传 tags:[] + 服务端 replaceTags 全量替换；且 list 查询不返回 tags → 页面看不到标签
  2. **已完成项目编辑必失败**：服务端"done 只能改说明和标签"，前端编辑只让改标题 → 必报 INVALID_STATE 且未捕获
  3. 编辑不完整（只改标题）、筛选未用满（status/goalId 服务端支持页面无 UI）、日期不展示无到期感知、新建无 rules/loading、无成功反馈、确认框取消未捕获、状态流转无防重复
- 产出 docs/prompts/P0项目体验修复任务书.md（R-1~R-8）：共用编辑弹窗+真实标签回填（核心）、服务端 list 补 tags 返回（对齐 goals service，最小改动）、done 项目禁用计划字段、筛选/到期感知/标签展示
- 与目标/待办修复同构，可三份并行分发

## 知识库多库结构（拍板 + 任务书交付，P1-4b）
- 用户确认语雀式效果（交互演示后："我要的就是这种效果"），要求落任务书
- 决策 K1~K7（docs/decisions/P1-知识库多库结构决策.md）：单 collection + kb_id/folder_id 标量过滤（不拆库/不用 partition）；collection 升级 knowledge_chunk_v2；存量迁移 exportAll(v1)→join document 补归属→importAll(v2)→count 校验才切换（不重嵌入）；默认库兜底；非空库/文件夹禁止删除；移动文档 MySQL+Milvus 双写同步；标签重命名全局生效
- 产出 docs/prompts/知识库多库结构任务书.md（D-1~D-6）：迁移 006（kb/folder 表 + document 加列 + 默认库初始化）、kb/文件夹 CRUD、list 扩展 kbId/folderId + moveDocument、Milvus v2（search filter + migrateV1toV2）、标签管理 + knowledge.storage 设置项、UI 重构（左库+文件夹树/右文档列表）+ 标签页
- 工程现状核实：下一迁移编号 006；chunk 表无归属列（走 document join）；Milvus search 无 filter 需扩展；tag/entity_tag 表已在（005）；knowledge.import 设置已存在
- 开发计划：新增 P1-4b 行（P1-4 与 P1-5 之间）；P1-5 RAG 任务书必须基于多库结构出（检索预留 kbId 过滤）

## 习惯模块审查 + 修复任务书
- 审查结论（服务端打卡/撤销/历史/streak 已验收且支持任意日期补卡，问题在前端 + 缺状态字段）：
  1. 无暂停/归档（habit 表无 status 字段，需迁移 007 + updateStatus）
  2. 无补卡入口（服务端 checkin 支持 checkedOn<=today，UI 只能当天）
  3. 编辑只改名称（服务端 update 全字段+改频率重算 streak 未暴露）
  4. 周目标无进度展示（weekly_times 只显示 streak）
  5. 历史弹窗只有日期列表（无日历/统计/补卡入口）
  6. 列表无排序；同款体验问题：无 rules/无反馈/取消未捕获/N+1（每习惯一次 history）/无防重复
- 产出 docs/prompts/P0习惯体验修复任务书.md（H-1~H-9）：表单校验+共用编辑弹窗+改频率二次确认、写操作反馈+取消捕获、list 返回 checkedToday 消除 N+1、日历补卡/撤销+统计、迁移 007 status(active/paused/archived)+updateStatus+归档隐藏、周进度 X/Y+达标标记、纯前端排序
- 服务端改动最小化：仅 list 扩展 + updateStatus + 迁移 007；checkin/undo/history/streak 一个不碰
