# 2026-08-19 工作日志

## 人生系统项目 — 《功能规格补充》（FSD）产出（④原型设计收官交付）

- 按功能规格任务书产出 `docs/functional-spec.md`（v1.0，约 1640 行），作为「⑤开发实现」的唯一需求依据。
- 通读 4 份必读材料：设计文档 v0.2、PRD&功能优先级、architecture.md、架构师任务书；另参照低保真原型 v0.1（仅视觉/交互参考，不构成范围边界）。
- **结构**：第 0 章通用约定（枚举映射表 UI↔库值、四态规范、确认分级、撤销、toast 文案风格、8 个错误码降级表、数据通用规则 9 条）+ 六大章节（①目标行动线 ②知识沉淀线 ③心理健康 ④项目交付 ⑤Dashboard 与全局层 ⑥设置与系统）+ 附 A 发布门验收索引 + 附 B 待确认项清单。
- **每页 6 部分统一**：功能点清单 / 表单字段规格（含字段↔schema 映射）/ 状态机与流转（含非法路径）/ 交互细节 / 边界与降级 / 验收要点。共 25 个小节全部六段式。
- **关键规格定义**（开发直接依赖）：
  - 状态机 11 组：goal（done↔active 可逆、done↔abandoned 互斥非法）、task（done→doing 非法）、inbox_item（pending 四出口 + bookmarked→clipped 后补剪藏）、document 双状态（parse_status + index_status 含 stale 重建）、timeline 升格单向、project_idea 漏斗（launched 终态）、dev_project status + 阶段前进轻操作/后退确认、周报生成运行态等。
  - 习惯 streak 算法：daily 逐日回溯清零；weekly 连续达标自然周、未结束周不中断。
  - Dashboard 焦点行动选取规则（最早到期→最近创建→空态 CTA）。
  - 提醒为运行时计算不建表，压制状态存 app_setting；后台任务不落表。
  - 情绪隐私边界落成 6 条可测规则（不进 FULLTEXT/Milvus/RAG/全局搜索/LLM RAG 请求，wellbeing 模块禁依赖向量模块）。
- **待确认项 18 条**（T1~T18，均给推荐未拍板）：生活项目入口、导入文件上限 50MB、失败原因持久化、软删除 30 天、语雀来源标签化、问答历史不持久化、周报覆盖、情绪词库清单、阶段历史需新表 T9、终点阶段=维护、usage_tags 用 entity_type='document_usage'、逾期归今天组等。
- 排除范围（本版不覆盖，待阶段启动补充）：洞察页、日/月/季度周期回顾、日历与时间预算（P2 后段）。

状态：待用户评审 FSD 与待确认项清单；确认后进入⑤开发实现阶段。
